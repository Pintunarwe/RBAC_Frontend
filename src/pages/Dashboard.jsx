import React from 'react';
import '../styles/Dashboard.css'

const Dashboard = () => (
  <div>
    <h1>Welcome to the RBAC Dashboard</h1>
    <p>Use the navigation bar to manage users, roles, and permissions.</p>
  </div>
);

export default Dashboard;
